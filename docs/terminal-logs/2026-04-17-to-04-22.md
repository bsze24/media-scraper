# Session log — April 17–22, 2026

## PRs created
| # | Branch | Title | Status |
|---|--------|-------|--------|
| 65 | `fix/reset-in-flight-button` | Reset In-Flight button on admin | Merged |
| 66 | `fix/youtube-no-captions-enoent` | Graceful ENOENT in YouTube scraper | Merged |
| 67 | ~~`feat/transcript-viewer-tweaks`~~ | Closed (branch renamed) | Closed |
| 68 | `feat/admin-polish-and-viewer-debug` | Admin table polish + viewer debug label | Merged |
| 69 | `feat/regression-snapshot-gaps` | Expand regression snapshot coverage | Merged |
| 70 | `fix/post-process-passages-correctness` | Speaker matching + overlap inversion fixes | Merged |
| 71 | `improve/post-process-passages-quality` | Sentence splits + gap triage | Merged |
| 72 | `feat/passages-db-layer` | Passages migration + types + queries | Open |
| 73 | `feat/persist-playback-speed` | Persist playback speed in saved views | Open |

## Pushed directly to main
- Tech debt #28 (mergeCorrectedTurns corruption) + #29 (merge speaker normalizers)
- `docs/tech-debt/028-merge-corrected-turns.md` detailed writeup

## Key decisions
- **Option B for PR A/B split**: PRs #70 and #71 are independent (both branch off main), can merge in either order. Will have merge conflict in `post-process-passages.ts` since both touch it — straightforward to resolve.
- **Tech debt #30** (passage gap correction policy) lives in PR #71, not #70
- **Migration 013** `appearance_id` corrected from `TEXT` to `UUID` (prompt had wrong type)
- **`PassageInsert`** uses `Omit<Passage, ...>` instead of separate interface; `SegmentStepOutput.passages` typed as `PassageInsert[]` not `Passage[]`

## Stash management
- Old stash (10 files, mixed passage + admin + viewer from Mar 30) → split, dropped
- Passage pipeline stash (5 files) → created, then dropped (rebuilding fresh in PRs #70-72)
- Tech debt #30 stash → popped into PR #71
- No active stashes remain

## Untracked file cleanup
- Deleted 3 stale files from Mar 30: `segment-passages.ts`, `test-segmentation-pipeline.ts`, `013_create_passages_table.sql`
- Remaining untracked: `.vscode/`, `appearances_prod.sql`, `colossus-439.html`, `docs/temp/`, utility scripts in `scripts/`

## Migration state
- **Dev**: migrations 001-012 applied (007 tracked by Supabase, rest manual). 013 NOT yet applied.
- **Prod**: migration 001 tracked by Supabase, 002-011 applied manually. **012 (`default_view_params`) NOT applied** — causes "save view" to fail. Needs: `ALTER TABLE appearances ADD COLUMN IF NOT EXISTS default_view_params TEXT;` in Supabase SQL editor.
- Supabase JS client can't run DDL — must use dashboard SQL editor or `supabase db push`

## Regression snapshots
- Script expanded with 10 new fields: `turn_text_hash`, `turns_with_section_anchor`, `attribution_source/derived/inferred/unset`, `corrected_turn_count`, `total_supporting_quotes`, `quotes_with_section_anchor`, `speaker_roles`, `processing_error/detail`
- Auto-diff compares against previous snapshot
- Fresh baselines captured: dev (40 appearances), prod (60 appearances), dated 2026-04-21

## Debugging
- Highlight duration showing 38s: correct — 57.6s at 1.5x playback = 38.4s effective
- Appearance `e5c1029e` stuck at "queued" — user hadn't hit process button

## Next steps
- Merge PRs #72-73 (work through BugBot issues)
- Apply migration 012 to prod (`ALTER TABLE appearances ADD COLUMN IF NOT EXISTS default_view_params TEXT;`)
- After PR #72 merges: apply migration 013 to dev via `supabase db push`, verify passages table, start PR1b
- PR1b: `segmentPassages` pipeline function + test script
